export async function getGenres() {
  const token = localStorage.getItem("token");

  const response = await fetch("/api/cinema/genres/", {
    headers: {
      Authorization: `Bearer ${token}`,
    },
  });

  if (!response.ok) {
    throw new Error("Failed to fetch genres");
  }

  return response.json();
}
