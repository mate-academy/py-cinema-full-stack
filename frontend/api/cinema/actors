export async function getActors() {
  const token = localStorage.getItem("token");

  const response = await fetch("/api/cinema/actors/", {
    headers: {
      Authorization: `Bearer ${token}`,
    },
  });

  if (!response.ok) {
    throw new Error("Failed to fetch actors");
  }

  return response.json();
}
